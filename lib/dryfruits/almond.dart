import 'package:flutter/material.dart';

class Almond extends StatelessWidget {
  const Almond({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      
    );
  }
}